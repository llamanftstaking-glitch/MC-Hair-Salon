import type { ReactNode } from "react";

export const metadata = {
  title: "Staff Portal — MC Hair Salon & Spa",
};

export default function StaffLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
